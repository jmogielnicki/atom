var config = require('./package.json');
var oldVersion = config.version.split('.');
var version = ~~oldVersion.pop();
var nextVersion = oldVersion.concat(version + 1).join('.');
config.version = nextVersion;
var json = JSON.stringify(config,0,2);
require('fs').writeFileSync('./package.json', json, 'utf8');
